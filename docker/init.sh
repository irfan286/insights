#!/bin/bash
# This file is no longer used for production.
# Production setup uses Docker image build + docker-compose.yml.
# See README or apps.json + Dockerfile for how to build the image.
echo "Use 'docker compose up -d' from this directory."
echo "To build the image first: see build instructions in Dockerfile."
